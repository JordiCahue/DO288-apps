#!/bin/bash
echo "Construimos fichero sendmail.py"
echo "print('Enviado correo a jordicahue@es.ibm.com')" >> ./sendmail.py

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
   echo "Making post-invocation changes..."
else
   echo "Error: assemble failed!"
fi

exit $rc
